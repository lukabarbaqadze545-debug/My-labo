import{i as e}from"./rolldown-runtime-Dd_uD5pT.js";import{n as t}from"./jsx-runtime-BpzPEenQ.js";import{t as n}from"./dexie-react-hooks-B6dch1n-.js";import{l as r,n as i,r as a}from"./types-DVaESQeg.js";var o=e(t(),1),s=[{id:`prefix-sums`,title:`Prefix Sums`,titleKa:`პრეფიქს-ჯამები`,category:`foundation`,priority:`essential`,order:1,whatIs:"A prefix sum array `p` where `p[i]` holds the sum of the first `i` elements. Built once in O(n), it turns any range-sum query `sum(l..r)` into the single subtraction `p[r+1] - p[l]`.",intuition:`Precompute every "sum from the start". The sum of a middle segment is then just the difference of two of those — the shared front cancels out.`,whyItWorks:"sum(l..r) = (sum of 0..r) - (sum of 0..l-1). Both operands are already stored, so the query touches two cells regardless of how wide the range is. Using `p` of length n+1 with `p[0] = 0` removes the special case at l = 0.",naive:`The obvious solution answers each query with a loop from l to r: O(n) per query, O(n·q) total. With n, q up to 2·10^5 that is 4·10^10 operations — far past the ~10^8 a second budget. Prefix sums move the work to a one-time O(n) pass, after which every query is O(1), for O(n + q) overall.`,whenToUse:[`Many range-sum (or range-count) queries over an array that does not change`,`You need "sum of elements in [l, r]" as a fast subroutine inside a larger algorithm`,`Counting how many values fall in a range, after bucketing them into an array`],signals:[`"answer q queries, each asking for the sum / average / count over a subarray"`,`"the array is given once and never modified"`,`"for every index, you need the total to its left"`,`A brute-force double loop where the inner loop only accumulates a sum`],walkthrough:[`Array a = [3, 1, 4, 1, 5], 0-indexed. Query: sum of a[1..3] (values 1, 4, 1 = 6).`,`Build p of length 6 with p[0] = 0.`,`p[1] = p[0] + a[0] = 3; p[2] = 3 + 1 = 4; p[3] = 4 + 4 = 8; p[4] = 8 + 1 = 9; p[5] = 9 + 5 = 14.`,`sum(1..3) = p[4] - p[1] = 9 - 3 = 6. Correct, in one subtraction.`,`A second query sum(0..4) = p[5] - p[0] = 14 - 0 = 14, again O(1).`],cpp:[{caption:`1D prefix sums with range queries`,code:`#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, q;
    cin >> n >> q;
    vector<long long> p(n + 1, 0);        // p[0] = 0, size n+1 avoids l==0 case
    for (int i = 0; i < n; i++) {
        long long x; cin >> x;
        p[i + 1] = p[i] + x;              // sums can overflow int — use long long
    }
    while (q--) {
        int l, r; cin >> l >> r;          // inclusive, 0-indexed
        cout << p[r + 1] - p[l] << "\\n";  // sum of a[l..r]
    }
}`},{caption:`2D prefix sums — sum over a submatrix in O(1)`,code:`// p[i][j] = sum of the rectangle from (0,0) to (i-1,j-1)
vector<vector<long long>> p(n + 1, vector<long long>(m + 1, 0));
for (int i = 0; i < n; i++)
    for (int j = 0; j < m; j++)
        p[i + 1][j + 1] = a[i][j] + p[i][j + 1] + p[i + 1][j] - p[i][j];

// sum of the rectangle with corners (r1,c1) and (r2,c2), inclusive:
auto rect = [&](int r1, int c1, int r2, int c2) {
    return p[r2 + 1][c2 + 1] - p[r1][c2 + 1] - p[r2 + 1][c1] + p[r1][c1];
};`}],time:`O(n) preprocessing, O(1) per query. 2D: O(n·m) build, O(1) per rectangle.`,space:`O(n) for the prefix array (O(n·m) in 2D). The original array can be discarded.`,mistakes:["Storing sums in `int` — a range of 2·10^5 values near 10^9 overflows. Use `long long`.","Off-by-one: with a length-n `p` you must special-case l = 0. A length-(n+1) `p` with p[0] = 0 does not.",`Applying it to an array that changes between queries — then you need a Fenwick tree instead.`,"2D: forgetting the `+ p[i][j]` inclusion–exclusion term, which is subtracted twice otherwise."],edgeCases:[`l == r: a single element, sum = p[r+1] - p[r] = a[r].`,`Empty range (if the problem allows l > r): define it as 0, i.e. p[l] - p[l].`,`All negatives: still correct — prefix sums make no sign assumption.`,`n == 0: p = [0], no query is valid, but the build does not crash.`],exercises:[`Given an array, answer: "is the sum of a[l..r] equal to zero?" in O(1) per query.`,`Count subarrays with sum exactly k using a hash map of prefix-sum frequencies (O(n)).`,`Given a 0/1 array, find the longest subarray with equal counts of 0 and 1.`],practice:[{name:`Codeforces 466C — Number of Ways`,tag:`CF 1700`,url:`https://codeforces.com/problemset/problem/466/C`},{name:`USACO 2016 Jan Bronze — Angry Cows (prefix idea)`,tag:`USACO Bronze`},{name:`CSES — Subarray Sums II`,tag:`CSES`,url:`https://cses.fi/problemset/task/1661`}],combineNote:`Prefix sums are the base layer for difference arrays (their inverse), for 2D range queries, and for the "count subarrays with property X" family when paired with a hash map. Binary search on a prefix array locates the shortest prefix reaching a target sum when all values are non-negative.`,prerequisites:[],related:[`difference-arrays`,`frequency-arrays`,`fenwick-tree`],combinesWith:[`binary-search`,`frequency-arrays`,`two-pointers`],next:[`difference-arrays`,`two-pointers`]},{id:`difference-arrays`,title:`Difference Arrays`,titleKa:`სხვაობის მასივები`,category:`foundation`,priority:`essential`,order:2,whatIs:"The inverse of a prefix sum. To add `v` to every element of `a[l..r]`, you instead do `d[l] += v` and `d[r+1] -= v` on a difference array `d`. After all updates, the prefix sum of `d` reconstructs the final array.",intuition:"Record only where the running increment *changes*: it turns on at `l` and turns off just past `r`. Sweeping left to right re-accumulates the value.",whyItWorks:"If `d` is defined so that `a[i] = d[0] + d[1] + ... + d[i]`, then a range update on `a` is a point update on `d`: raising the increment at `l` lifts every later prefix, and lowering it at `r+1` cancels that lift from `r+1` on. Exactly the elements `l..r` keep the change.",naive:`Applying q range-add updates directly is O(n) each, O(n·q) total — too slow for n, q ~ 2·10^5. The difference array makes each update O(1); one final O(n) prefix-sum pass materialises the array. Total O(n + q).`,whenToUse:[`Many range-add / range-subtract updates, and you only need the array *after* all of them`,`"Add 1 to every position in [l, r]" repeated — e.g. counting overlapping intervals`,`Simulating +/- events on a timeline (people entering and leaving, bookings)`],signals:[`"apply q updates, each adding a value to a contiguous range, then print the array"`,`"how many intervals cover each point"`,`"the queries are all updates; there are no reads until the end"`,`Range updates with only a single final read — no interleaved queries`],walkthrough:[`n = 5, array starts [0,0,0,0,0]. Updates: add 2 to [1,3], then add 5 to [0,2].`,`d starts [0,0,0,0,0,0] (length n+1).`,`Update 1: d[1] += 2, d[4] -= 2  →  d = [0, 2, 0, 0, -2, 0].`,`Update 2: d[0] += 5, d[3] -= 5  →  d = [5, 2, 0, -5, -2, 0].`,`Prefix-sum d: [5, 7, 7, 2, 0, ...] — take the first n: a = [5, 7, 7, 2, 0].`,`Check position 2: covered by both updates, 2 + 5 = 7. Correct.`],cpp:[{caption:`Range-add updates, one final materialisation`,code:`#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, q;
    cin >> n >> q;
    vector<long long> d(n + 1, 0);
    while (q--) {
        int l, r; long long v;
        cin >> l >> r >> v;          // add v to a[l..r], inclusive
        d[l] += v;
        d[r + 1] -= v;               // safe: d has size n+1
    }
    long long cur = 0;
    for (int i = 0; i < n; i++) {
        cur += d[i];                 // prefix sum reconstructs a[i]
        cout << cur << " ";
    }
}`},{caption:`2D difference array — add v to a submatrix in O(1)`,code:`// stamp the rectangle (r1,c1)-(r2,c2) with +v
d[r1][c1]         += v;
d[r1][c2 + 1]     -= v;
d[r2 + 1][c1]     -= v;
d[r2 + 1][c2 + 1] += v;
// then take a 2D prefix sum of d to get the final grid`}],time:`O(1) per range update, O(n) for the single final pass. Total O(n + q).`,space:`O(n) for the difference array (O(n·m) in 2D).`,mistakes:["Sizing `d` as length n and then writing `d[r+1]` when r = n-1 — out of bounds. Use n+1.",`Reading a value mid-stream: the array is only valid after the prefix-sum pass. Interleaved reads need a Fenwick tree.`,"Forgetting `long long` when many updates stack on one cell.",`2D: getting the inclusion–exclusion signs wrong on the four corner stamps.`],edgeCases:["r == n-1: the `-v` lands at d[n], which exists only because d has size n+1.",`l == r: a single-element update, d[l] += v and d[l+1] -= v.`,`Overlapping updates: they simply add — no special handling.`,`v negative: range subtraction, identical mechanics.`],exercises:[`Given q intervals [l, r], output for each position how many intervals cover it.`,`Simulate a car-pooling schedule: given (passengers, start, end) trips, does capacity hold at every point?`,`Apply range updates, then answer a few range-sum queries — combine with prefix sums.`],practice:[{name:`CSES — Range Update Queries (offline variant)`,tag:`CSES`},{name:`LeetCode 1109 — Corporate Flight Bookings`,tag:`LC Medium`,url:`https://leetcode.com/problems/corporate-flight-bookings/`},{name:`LeetCode 1094 — Car Pooling`,tag:`LC Medium`,url:`https://leetcode.com/problems/car-pooling/`}],combineNote:`Difference array in, prefix sum out — they are one technique used in two directions. In 2D, a difference array plus a 2D prefix sum gives O(1) submatrix updates. When reads and updates interleave, this pattern is what a Fenwick tree generalises.`,prerequisites:[`prefix-sums`],related:[`prefix-sums`,`fenwick-tree`],combinesWith:[`prefix-sums`,`sorting-techniques`],next:[`frequency-arrays`,`fenwick-tree`]},{id:`frequency-arrays`,title:`Frequency Arrays / Counting`,titleKa:`სიხშირის მასივები`,category:`foundation`,priority:`essential`,order:3,whatIs:'An array `cnt` indexed by *value* rather than position: `cnt[x]` is how many times `x` appears. Building it is one O(n) pass; afterwards any "how many of value x" question is O(1), and a prefix sum over `cnt` answers "how many values ≤ x".',intuition:`Stop scanning the data to answer questions about it. Bucket each element once, then read the buckets.`,whyItWorks:`Array indexing is O(1). If the value range is small enough to be an index (say 0..10^6), a direct-address table beats a hash map on constant factor and beats sorting on asymptotics for counting tasks. Counting sort and histogram queries are the same idea.`,naive:`Answering "how many elements equal x" or "how many are ≤ x" by scanning is O(n) per query. Sorting first gives O(n log n) then O(log n) per query via binary search. A frequency array gives O(n + V) build (V = value range) and O(1) per query — better when V is not much larger than n.`,whenToUse:[`Values lie in a known, modest range (fits as an array index)`,`You need counts, "k-th smallest", or "number of values in [lo, hi]" repeatedly`,`Counting sort: stable sort of small integers in O(n + V)`,`Character counts (fixed alphabet of 26 or 128)`],signals:[`"array elements are between 1 and 10^6" — a bounded value range`,`"count pairs / elements with value equal to ..." `,`"how many numbers are less than or equal to x"`,`"anagram", "permutation", "same multiset" — compare frequency arrays`],walkthrough:[`a = [2, 5, 2, 1, 5, 2], values in 0..5.`,`cnt after one pass: cnt[1] = 1, cnt[2] = 3, cnt[5] = 2, rest 0.`,`"How many 2s?"  → cnt[2] = 3, O(1).`,`Prefix-sum cnt → pre = [0, 1, 4, 4, 4, 6] (pre[v] = count of values ≤ v).`,`"How many values ≤ 3?"  → pre[3] = 4.`,`"3rd smallest element?"  → smallest v with pre[v] ≥ 3 is v = 2 (pre[2] = 4).`],cpp:[{caption:`Frequency array + prefix counts`,code:`#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    const int V = 1000001;              // one past the max value
    vector<int> cnt(V, 0);
    for (int i = 0; i < n; i++) {
        int x; cin >> x;
        cnt[x]++;
    }
    // prefix counts: pre[v] = number of elements <= v
    vector<long long> pre(V, 0);
    pre[0] = cnt[0];
    for (int v = 1; v < V; v++) pre[v] = pre[v - 1] + cnt[v];

    // number of elements in [lo, hi]:
    auto inRange = [&](int lo, int hi) {
        return pre[hi] - (lo > 0 ? pre[lo - 1] : 0);
    };
    (void)inRange;
}`},{caption:`Counting sort — stable, O(n + V)`,code:`vector<int> countingSort(const vector<int>& a, int V) {
    vector<int> cnt(V + 1, 0);
    for (int x : a) cnt[x]++;
    for (int v = 1; v <= V; v++) cnt[v] += cnt[v - 1];   // cnt[v] = final end index
    vector<int> out(a.size());
    for (int i = (int)a.size() - 1; i >= 0; i--)         // reverse pass keeps it stable
        out[--cnt[a[i]]] = a[i];
    return out;
}`}],time:`O(n + V) to build, O(1) per exact-count query, O(1) per range-count after a prefix pass.`,space:`O(V) — the value range, not the input size. This is the constraint that decides applicability.`,mistakes:[`Negative or offset values: shift by a constant so the minimum maps to index 0.`,`V too large to allocate (values up to 10^18) — fall back to a hash map or coordinate compression.`,"Using `int` for prefix counts when n is large and many elements share a value.",`Counting sort: a forward final pass instead of reverse breaks stability.`],edgeCases:[`A value that never appears: cnt[x] = 0, queries still correct.`,`All elements equal: cnt has a single non-zero bucket.`,`x outside [0, V): must be filtered or the index is invalid.`,`n = 0: cnt is all zeros, every count query returns 0.`],exercises:[`Check if two strings are anagrams by comparing 26-length frequency arrays.`,`Given an array with values in [1, 100], answer 10^5 queries of "count of values in [lo, hi]".`,`Find the smallest missing positive integer using a boolean frequency array.`],practice:[{name:`CSES — Distinct Numbers (counting / set)`,tag:`CSES`,url:`https://cses.fi/problemset/task/1621`},{name:`Codeforces 977C — Less or Equal`,tag:`CF 1200`,url:`https://codeforces.com/problemset/problem/977/C`},{name:`LeetCode 242 — Valid Anagram`,tag:`LC Easy`,url:`https://leetcode.com/problems/valid-anagram/`}],combineNote:`A frequency array *is* a prefix-sum problem waiting to happen: build counts, then prefix them for range/order-statistic queries. When the value range is too big, coordinate compression shrinks it back into an index. Sliding-window problems over a bounded alphabet keep a live frequency array as the window moves.`,prerequisites:[`prefix-sums`],related:[`prefix-sums`,`coordinate-compression`,`sorting-techniques`],combinesWith:[`sliding-window`,`prefix-sums`,`two-pointers`],next:[`two-pointers`,`sorting-techniques`]},{id:`two-pointers`,title:`Two Pointers`,titleKa:`ორი მაჩვენებელი`,category:`foundation`,priority:`essential`,order:4,whatIs:`Two indices that move through a sequence in a coordinated way — often from both ends toward the middle, or both forward at different speeds — so the work is one linear pass instead of a nested loop.`,intuition:`When advancing one pointer can only ever push the other in one direction, you never need to go back. The double loop collapses because most (i, j) pairs can be skipped without checking.`,whyItWorks:`It relies on monotonicity. Example: in a sorted array, if a[i] + a[j] is too large, then pairing a[i] with anything above a[j] is also too large, so move j down; if too small, move i up. Each pointer travels one way only, so the total number of moves is at most 2n even though the pair space is quadratic.`,naive:`Checking all pairs (i, j) to find one with a target property is O(n^2). Two pointers exploit an ordering so that after comparing (i, j) you know which pointer to move, discarding a whole row or column of the pair matrix each step. That drops O(n^2) to O(n) (plus O(n log n) if a sort is needed first).`,whenToUse:[`The array (or the relevant key) is sorted, or can be sorted`,`Searching for a pair / triple with a sum or difference condition`,`Merging two sorted sequences`,`Partitioning in place (Dutch-flag, quicksort partition)`,`Comparing / matching two sequences (is s a subsequence of t?)`],signals:[`"sorted array" + "find two elements such that ..."`,`"pair with sum equal to / closest to target"`,`"merge", "in place", "without extra space"`,`"is one string a subsequence of another"`,`A brute-force pair search where the array happens to be sorted`],walkthrough:[`Sorted a = [1, 3, 4, 6, 8, 11], target sum = 10.`,`i = 0 (val 1), j = 5 (val 11). 1 + 11 = 12 > 10 → move j left.`,`i = 0 (1), j = 4 (8). 1 + 8 = 9 < 10 → move i right.`,`i = 1 (3), j = 4 (8). 3 + 8 = 11 > 10 → move j left.`,`i = 1 (3), j = 3 (6). 3 + 6 = 9 < 10 → move i right.`,`i = 2 (4), j = 3 (6). 4 + 6 = 10 → found. Total pointer moves: 5, not 15 pair checks.`],cpp:[{caption:`Opposite ends: pair with a given sum in a sorted array`,code:`#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; long long target;
    cin >> n >> target;
    vector<long long> a(n);
    for (auto& x : a) cin >> x;
    sort(a.begin(), a.end());

    int i = 0, j = n - 1;
    while (i < j) {
        long long s = a[i] + a[j];
        if (s == target) { cout << i << " " << j << "\\n"; return 0; }
        if (s < target) i++;      // need a bigger sum
        else j--;                 // need a smaller sum
    }
    cout << "no pair\\n";
}`},{caption:`Same direction: is s a subsequence of t?`,code:`bool isSubsequence(const string& s, const string& t) {
    int i = 0;                          // pointer into s
    for (int j = 0; j < (int)t.size() && i < (int)s.size(); j++)
        if (t[j] == s[i]) i++;          // matched one char of s
    return i == (int)s.size();
}`}],time:`O(n) for the scan, O(n log n) overall when the input must be sorted first.`,space:`O(1) extra — the pointers only. Sorting may add O(log n) stack for the recursion.`,mistakes:[`Using two pointers on unsorted data when the logic needs monotonicity — the answer is silently wrong.`,"Loop condition `i <= j` when a pair needs two distinct indices — use `i < j`.",`Moving both pointers in the same step and skipping the valid pair between them.`,`Forgetting that after sorting, the returned indices are into the *sorted* array, not the original.`],edgeCases:[`n < 2: no pair exists — return early.`,"Duplicate values equal to target/2: make sure `i < j` still allows the pair.",`All elements identical: pointers meet in the middle correctly.`,`Target unreachable: pointers cross, loop exits, report "none".`],exercises:[`Given a sorted array, count pairs with sum < target in O(n).`,`Merge two sorted arrays into one without a library merge.`,`Move all zeros to the end of an array in place, keeping the order of non-zeros.`,`3-sum: fix one element, two-pointer the rest — O(n^2) total.`],practice:[{name:`CSES — Sum of Two Values`,tag:`CSES`,url:`https://cses.fi/problemset/task/1640`},{name:`Codeforces 279B — Books`,tag:`CF 1400`,url:`https://codeforces.com/problemset/problem/279/B`},{name:`LeetCode 15 — 3Sum`,tag:`LC Medium`,url:`https://leetcode.com/problems/3sum/`}],combineNote:`Two pointers is the parent pattern of the sliding window (both pointers move forward, the window is the gap between them). It pairs with sorting almost always, and with a frequency array when the "condition" is about how many distinct values sit between the pointers. Fixing one index and two-pointering the rest is the standard way to add a dimension (2Sum → 3Sum).`,prerequisites:[`frequency-arrays`],related:[`sliding-window`,`sorting-techniques`,`binary-search`],combinesWith:[`sorting-techniques`,`frequency-arrays`,`binary-search`],next:[`sliding-window`,`binary-search`]},{id:`sliding-window`,title:`Sliding Window`,titleKa:`მოცურავე ფანჯარა`,category:`foundation`,priority:`essential`,order:5,whatIs:"A contiguous subarray `[l, r]` maintained as `r` advances one step at a time. `l` is pulled forward only as far as needed to keep the window valid. Some aggregate (sum, distinct count, max frequency) is updated incrementally on each move rather than recomputed.",intuition:"The answer for window ending at `r` reuses almost all of the work done for `r - 1`: one element enters on the right, zero or more leave on the left.",whyItWorks:'For "longest/shortest valid window" problems, validity is monotone in `l`: if `[l, r]` is valid then `[l+1, r]` is too (or the reverse). So `l` never needs to move backward — across the whole run it advances at most n times, and `r` advances exactly n times. O(n) even though the window resizes.',naive:`Enumerating every subarray and checking it is O(n^2) subarrays × O(n) check = O(n^3), or O(n^2) with a prefix sum. The sliding window keeps a running summary of the current window and slides it, so each element is added once and removed once: O(n) total.`,whenToUse:[`"Longest / shortest / count of contiguous subarrays such that <condition>"`,`The condition is monotone: growing the window can only make it "more invalid", shrinking "more valid" (or vice versa)`,`Fixed-size window: "every subarray of length k" — a special, simpler case`,`All values non-negative (for sum conditions) — negatives break monotonicity, use prefix sum + map instead`],signals:[`"longest substring / subarray with at most k distinct ..."`,`"smallest subarray with sum ≥ target"`,`"maximum sum of any k consecutive elements"`,`"contiguous" + an aggregate condition + non-negative values`],walkthrough:[`a = [2, 1, 5, 1, 3, 2], find the shortest subarray with sum ≥ 7.`,`r = 0: win sum 2. r = 1: 3. r = 2: 8 ≥ 7 → try to shrink: drop a[0]=2 → 6 < 7, stop. best len = 3 ([2,1,5]).`,`r = 3: sum 6 + 1 = 7 ≥ 7 → shrink: drop a[1]=1 → 6, stop. window [5,1] ... wait sum is 5+1+1=7, drop a[1] gives 5+1+1? re-track: l=1 now, sum a[1..3]=1+5+1=7, drop a[1]=1 → a[2..3]=6 <7 stop. len 3.`,`r = 4: add 3 → a[2..4] = 5+1+3 = 9 ≥ 7 → shrink: drop a[2]=5 → 4 < 7 stop. len 3.`,`r = 5: add 2 → a[3..5] = 1+3+2 = 6 < 7. window stays.`,`Answer: length 3. Each index entered and left the window once.`],cpp:[{caption:`Variable window: shortest subarray with sum ≥ target`,code:`#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; long long target;
    cin >> n >> target;
    vector<long long> a(n);
    for (auto& x : a) cin >> x;

    long long sum = 0;
    int l = 0, best = INT_MAX;
    for (int r = 0; r < n; r++) {
        sum += a[r];                       // element enters on the right
        while (sum - a[l] >= target) {     // shrink while still valid
            sum -= a[l];
            l++;
        }
        if (sum >= target) best = min(best, r - l + 1);
    }
    cout << (best == INT_MAX ? -1 : best) << "\\n";
}`},{caption:`Variable window: longest substring with at most k distinct characters`,code:`int longestAtMostKDistinct(const string& s, int k) {
    vector<int> cnt(256, 0);
    int distinct = 0, l = 0, best = 0;
    for (int r = 0; r < (int)s.size(); r++) {
        if (cnt[(unsigned char)s[r]]++ == 0) distinct++;
        while (distinct > k) {                       // window invalid — shrink
            if (--cnt[(unsigned char)s[l]] == 0) distinct--;
            l++;
        }
        best = max(best, r - l + 1);
    }
    return best;
}`}],time:`O(n): every index is added once and removed at most once. O(1) or O(alphabet) per step.`,space:`O(1) for a numeric aggregate; O(alphabet) or O(distinct) if a frequency map tracks the window.`,mistakes:[`Using a window when values can be negative — a longer window is no longer guaranteed to have a larger sum. Use prefix sums + a hash map.`,`Recomputing the aggregate from scratch each step — that is back to O(n^2).`,"Shrinking with `if` when a single entering element can invalidate the window by more than one — use `while`.","Fixed-size window: forgetting to remove `a[r-k]` when `r ≥ k`.","Off-by-one in the length `r - l + 1`."],edgeCases:[`No valid window ever forms — return -1 / 0 as the problem dictates.`,"The whole array is the answer — `l` never moves.",`k = 0 for "at most k distinct" — only empty windows are valid.`,`Single element ≥ target — window of length 1.`,"All elements zero with target 0 — the shrink condition must be `>` not `≥` to avoid an empty window, depending on the exact task."],exercises:[`Maximum sum of any window of fixed size k.`,`Longest substring without repeating characters.`,`Count subarrays with exactly k distinct integers (hint: atMost(k) - atMost(k-1)).`,`Smallest window in s that contains all characters of t.`],practice:[{name:`CSES — Playlist (longest distinct window)`,tag:`CSES`,url:`https://cses.fi/problemset/task/1141`},{name:`Codeforces 1234D — Distinct Characters Queries`,tag:`CF 1600`,url:`https://codeforces.com/problemset/problem/1234/D`},{name:`LeetCode 76 — Minimum Window Substring`,tag:`LC Hard`,url:`https://leetcode.com/problems/minimum-window-substring/`}],combineNote:`The sliding window is two pointers moving the same direction with the gap as the object of interest. It keeps a live frequency array for alphabet conditions. The "exactly k" trick reduces to two "at most" windows. When the window aggregate is a max/min, a monotonic deque upgrades each step back to amortised O(1).`,prerequisites:[`two-pointers`],related:[`two-pointers`,`frequency-arrays`,`monotonic-queue`],combinesWith:[`frequency-arrays`,`prefix-sums`,`monotonic-queue`],next:[`binary-search`,`monotonic-queue`]},{id:`binary-search`,title:`Binary Search`,titleKa:`ბინარული ძებნა`,category:`foundation`,priority:`essential`,order:6,whatIs:"Repeatedly halve a search interval over a monotone predicate. If `f` is false, false, ..., false, true, ..., true along the range, binary search finds the false→true boundary in `O(log n)` probes instead of scanning.",intuition:`One probe at the midpoint tells you which half the answer is in, so half the candidates disappear every step. You never look at both halves.`,whyItWorks:'The predicate is monotone: once it flips from false to true it never flips back, so the midpoint check is decisive — it eliminates an entire side. `log2(n)` halvings take `n` candidates down to 1. `lower_bound` and `upper_bound` are this idea applied to "first element ≥ x" and "first > x".',naive:"Scanning for the first element ≥ x, or the first index where a condition holds, is `O(n)`. Done once per query or per candidate it becomes `O(n·q)` or `O(n^2)` — too slow for `n, q ~ 2·10^5`. Binary search makes each such lookup `O(log n)`, for `O(q log n)` or `O(n log n)` overall.",whenToUse:[`Sorted array + "find / count / locate value x or the boundary of a condition"`,`A monotone yes/no predicate over an integer range — the answer is "the smallest x that works"`,"`lower_bound` / `upper_bound` — first ≥ x, first > x; their difference is a count","Real-valued feasibility that is monotone — binary search on a `double` for a fixed iteration count"],signals:[`"sorted array" together with "find", "count occurrences of", "closest to"`,`"smallest / largest value such that <condition holds>"`,`"the array is sorted (or can be sorted) and you query it many times"`,`A monotone check where you want the exact tipping point`],walkthrough:[`Sorted a = [2, 4, 4, 7, 9, 13]. Find the first index with a[i] ≥ 8.`,`Half-open interval [lo, hi) = [0, 6); the answer lives somewhere in [0, 6].`,`mid = 3, a[3] = 7 < 8 → the answer is to the right → lo = 4.`,`mid = 5, a[5] = 13 ≥ 8 → the answer is here or left → hi = 5.`,`mid = 4, a[4] = 9 ≥ 8 → hi = 4.`,`lo == hi == 4 → first index with a[i] ≥ 8 is 4. Three probes, not six.`],cpp:[{caption:`Boundary search on a sorted array (hand-rolled lower_bound)`,code:`#include <bits/stdc++.h>
using namespace std;

// first index i in [0, n) with a[i] >= x, or n if none
int lowerBound(const vector<long long>& a, long long x) {
    int lo = 0, hi = (int)a.size();          // half-open: answer in [lo, hi]
    while (lo < hi) {
        int mid = lo + (hi - lo) / 2;        // never (lo + hi) / 2 — overflow
        if (a[mid] >= x) hi = mid;            // mid still a candidate
        else lo = mid + 1;                    // mid ruled out
    }
    return lo;
}

int main() {
    vector<long long> a = {2, 4, 4, 7, 9, 13};
    int lb = lowerBound(a, 4);                // 1
    int ub = lowerBound(a, 5);                // 3  -> count of 4s = ub - lb = 2
    // library equivalents:
    // lower_bound(a.begin(), a.end(), 4) - a.begin();
    // upper_bound(a.begin(), a.end(), 4) - a.begin();
    cout << lb << " " << ub << "
";
}`},{caption:`Generic "first true" template over an integer range`,code:`// smallest m in [lo, hi] with ok(m) true, assuming ok is false...false,true...true
long long firstTrue(long long lo, long long hi, function<bool(long long)> ok) {
    hi++;                                     // hi becomes an exclusive "definitely true" sentinel
    while (lo < hi) {
        long long mid = lo + (hi - lo) / 2;
        if (ok(mid)) hi = mid;
        else lo = mid + 1;
    }
    return lo;                                // == original hi + 1 if nothing works
}`}],time:`O(log n) per search. O(n log n) if the array must be sorted first. Real-valued: a fixed ~100 iterations, or O(log((hi − lo) / eps)).`,space:`O(1) extra.`,mistakes:["`mid = (lo + hi) / 2` overflows `int` when lo + hi exceeds INT_MAX. Always `lo + (hi - lo) / 2`.","Mixing interval conventions — `[lo, hi]` inclusive vs `[lo, hi)` half-open — causes infinite loops or off-by-one. Pick one and keep it for the whole function.",`Binary searching a predicate that is not monotone — the result is silently wrong, not a crash.`,"`lower_bound` returns `end()` when nothing qualifies; dereferencing it is undefined behaviour.","Real-valued: looping while `lo < hi` on doubles may never terminate — loop a fixed number of times."],edgeCases:[`Empty array — returns index 0 / "not found".`,"All elements < x — `lower_bound` returns n.",`All elements ≥ x — returns 0.`,"Duplicates of x — `lower_bound` gives the first, `upper_bound` the position after the last.",`Single element — one probe decides it.`],exercises:["Count occurrences of x in a sorted array as `upper_bound - lower_bound`.",`Find the element of a sorted array closest to x.`,`Find the peak of a bitonic array (increasing then decreasing) by binary searching the slope.`,`Decide whether x appears in a matrix whose rows and columns are each sorted.`],practice:[{name:`LeetCode 34 — Find First and Last Position`,tag:`LC Medium`,url:`https://leetcode.com/problems/find-first-and-last-position-of-element-in-sorted-array/`},{name:`Codeforces 706B — Interesting drink`,tag:`CF 900`,url:`https://codeforces.com/problemset/problem/706/B`},{name:`CSES — Concert Tickets`,tag:`CSES`,url:`https://cses.fi/problemset/task/1091`}],combineNote:'Binary search is the lookup under `lower_bound` / `upper_bound`, so it pairs with sorting on nearly every "sort then query" problem. On a prefix-sum array of non-negative values it finds the shortest prefix reaching a target. Generalised to the answer itself — binary search on the answer — it turns an optimisation into a feasibility check. Coordinate compression is exactly `lower_bound` on the sorted list of distinct values.',prerequisites:[],related:[`two-pointers`,`sorting-techniques`,`binary-search-on-answer`,`prefix-sums`],combinesWith:[`prefix-sums`,`sorting-techniques`,`coordinate-compression`],next:[`binary-search-on-answer`,`ternary-search`]},{id:`bit-manipulation`,title:`Bit Manipulation`,titleKa:`ბიტ-მანიპულაცია`,category:`foundation`,priority:`important`,order:7,whatIs:"Treat an integer as an array of bits and use AND / OR / XOR / shifts to test, set, clear, or count them in `O(1)`. A subset of a universe of up to 64 elements fits in one `long long`.",intuition:`Bit i answers a yes/no question about element i. One machine word holds 64 such answers and one instruction updates or combines all of them at once.`,whyItWorks:"The CPU operates on every bit of a word in parallel. `x & (1LL << i)` isolates bit i; `x ^ y` flips exactly the bits where x and y differ; `x & -x` isolates the lowest set bit because two’s-complement negation is flip-then-add-one; `__builtin_popcountll(x)` counts set bits in one instruction.",naive:"Storing a small subset as a `vector<bool>` or `set<int>` costs `O(n)` per union / intersection / membership plus allocation and cache misses. A bitmask does each in `O(1)` with no allocation — often the difference between an `O(2^n · n)` enumeration fitting the time limit and not.",whenToUse:[`A subset of a small universe — n ≤ ~22 to enumerate, ≤ 64 to store`,"Fast set algebra: union `|`, intersection `&`, difference `& ~`, symmetric difference `^`, membership `>> i & 1`",`Parity / XOR tricks: "the one non-repeated value", prefix XOR for range XOR`,`Counting set bits, iterating set bits, iterating submasks of a mask`],signals:[`"n ≤ 20" (often ≤ 22) — a strong hint that subsets are enumerated`,`"each element is taken or not", "on/off", "subset", "mask"`,`"every value appears twice except one" → XOR everything together`,`"toggle", "flip", "XOR of a range"`],walkthrough:[`mask = 0b1011 — elements 0, 1, 3 present in a universe of 4.`,"Has element 2?  `mask >> 2 & 1` = 0 → no.","Add element 2:  `mask | (1 << 2)` = 0b1111.","Remove element 0: `mask & ~(1 << 0)` = 0b1110.","Count: `__builtin_popcount(0b1110)` = 3.","Lowest set bit of 0b1110: `mask & -mask` = 0b0010 (element 1).","Iterate all 2^4 subsets: `for (int s = 0; s < (1 << 4); s++)`."],cpp:[{caption:`The idiom table`,code:`int  test  (int x, int i) { return (x >> i) & 1; }        // bit i set?
int  setBit(int x, int i) { return x | (1 << i); }
int  clr   (int x, int i) { return x & ~(1 << i); }
int  toggle(int x, int i) { return x ^ (1 << i); }
int  lowbit(int x)        { return x & -x; }               // lowest set bit (as a value)
int  count (long long x)  { return __builtin_popcountll(x); }

// iterate the set bits of x
for (int m = x; m; m &= m - 1) {
    int i = __builtin_ctz(m);   // index of the lowest set bit
    /* use element i */
}`},{caption:`XOR to find the unique element; iterate all submasks`,code:`// every value appears twice except one:
long long only = 0;
for (long long v : a) only ^= v;          // pairs cancel, the loner remains

// iterate every submask of mask (including 0), in decreasing order:
for (int sub = mask; ; sub = (sub - 1) & mask) {
    /* use sub */
    if (sub == 0) break;
}`}],time:`O(1) per bit operation. Enumerate all subsets of n: O(2^n). Iterate all submasks of every mask: O(3^n) total. popcount is O(1).`,space:`O(1) per mask (one word). An array indexed by mask is O(2^n).`,mistakes:["`1 << i` is a 32-bit `int`; for i ≥ 31 you need `1LL << i`. The single most common bit bug.","Precedence: `x & 1 == 0` parses as `x & (1 == 0)`. Write `(x & 1) == 0`.","`x & -x` on a plain `int` when x can be INT_MIN is undefined behaviour — use `long long` or unsigned.","Shifting by ≥ the type width (≥ 32 for `int`, ≥ 64 for `long long`) is undefined.",`Signed right shift of a negative number fills with the sign bit — use unsigned for logical shifts.`],edgeCases:["Empty mask (0) — popcount 0, no bits to iterate, `x & -x` = 0.","Full mask — for n ≥ 31 it must be `(1LL << n) - 1`.","n = 0 — the only subset is empty; the loop `s < 1` runs once.",`Negative inputs — bit ops see the two’s-complement representation.`],exercises:[`Given an array where every number appears twice except one, find it in O(n) time and O(1) space.`,`Generate all subsets of {0..n−1}, printing each as a list of elements.`,`For a fixed mask, enumerate its submasks in decreasing order.`,`Compute the XOR of a[l..r] for many queries using a prefix-XOR array.`],practice:[{name:`LeetCode 136 — Single Number`,tag:`LC Easy`,url:`https://leetcode.com/problems/single-number/`},{name:`LeetCode 78 — Subsets`,tag:`LC Medium`,url:`https://leetcode.com/problems/subsets/`},{name:`Codeforces 1362C — Johnny and Another Rating Drop`,tag:`CF 1400`,url:`https://codeforces.com/problemset/problem/1362/C`}],combineNote:"Bit manipulation is the substrate for bitmask enumeration (loop s over 0..2^n) and bitmask DP (state = the subset already handled). Prefix XOR gives range XOR the way prefix sum gives range sum. `x & -x` is the index step of a Fenwick tree. Meet in the middle splits the mask into two halves and recombines them.",prerequisites:[],related:[`bitmask-enumeration`,`bitmask-dp`,`frequency-arrays`],combinesWith:[`bitmask-enumeration`,`bitmask-dp`,`meet-in-the-middle`,`fenwick-tree`],next:[`bitmask-enumeration`]},{id:`recursion`,title:`Recursion (multi-branch)`,titleKa:`რეკურსია`,category:`foundation`,priority:`essential`,order:8,whatIs:`A function that solves a problem by calling itself on smaller instances and combining the results. "Multi-branch" means each call spawns several recursive calls — exploring a tree of choices rather than a single chain.`,intuition:`Express the answer for size n in terms of answers for smaller sizes, plus a base case that needs no recursion. The call stack remembers "where was I".`,whyItWorks:`If every call is on a strictly smaller instance and the base case is reachable, the recursion terminates; correctness follows by induction — assume the recursive calls are right, show the combine step is right. The call stack holds one frame per active call, so depth, not the total number of calls, is the memory cost.`,naive:`The real "naive vs optimised" here is recomputation. A multi-branch recursion that revisits the same subproblem is exponential — naive Fibonacci is O(phi^n) because fib(n−2) is recomputed all the way down. Caching each distinct subproblem (memoisation) makes it O(number of states). That single step is the doorway to dynamic programming.`,whenToUse:[`"Try every choice at each step" with small n — permutations, subsets, board placements`,`A tree or nested structure — recurse into the children`,`Divide and conquer — split, solve both halves, merge (merge sort, quickselect)`,`The definition is itself recursive: "an expression is a number, or (expr op expr)"`],signals:[`"generate all", "count the number of ways", "every possible" with small n`,`"the input is a tree", nested brackets, nested folders`,`"at each step you choose one of k options"`,`A definition that refers to itself`],walkthrough:[`Count monotone paths (right/down only) across a 2×2 grid of cells.`,`solve(r, c): at the goal → 1; out of bounds → 0; else solve(r+1, c) + solve(r, c+1).`,`solve(0,0) = solve(1,0) + solve(0,1)   — two branches.`,`solve(1,0) = solve(1,1) + solve(2,0) = 1 + 0 = 1.`,`solve(0,1) = solve(1,1) + solve(0,2) = 1 + 0 = 1.`,`solve(0,0) = 2. Note solve(1,1) was evaluated twice — on a large grid that overlap is why memoisation matters.`],cpp:[{caption:`Multi-branch recursion: include / exclude to list all subsets`,code:`#include <bits/stdc++.h>
using namespace std;

vector<int> cur;
void gen(const vector<int>& a, int i) {
    if (i == (int)a.size()) {                 // base case: a full decision made
        for (int x : cur) cout << x << ' ';
        cout << "
";
        return;
    }
    gen(a, i + 1);                            // branch 1: skip a[i]
    cur.push_back(a[i]);
    gen(a, i + 1);                            // branch 2: take a[i]
    cur.pop_back();                           // undo before returning
}`},{caption:`The recomputation trap and its fix`,code:`long long fibSlow(int n) {                    // O(phi^n): fib(n-2) recomputed everywhere
    if (n < 2) return n;
    return fibSlow(n - 1) + fibSlow(n - 2);
}

long long memo[100];                          // -1 = not computed yet
long long fibFast(int n) {                    // O(n): each state solved once
    if (n < 2) return n;
    long long& r = memo[n];
    if (r != -1) return r;
    return r = fibFast(n - 1) + fibFast(n - 2);
}`}],time:`With b branches and depth d the call count is O(b^d). Merge sort is O(n log n). A multi-branch recursion with overlapping subproblems is exponential until memoised, then O(states · work per state).`,space:`O(depth) for the call stack — this is the number that overflows, not the call count. Depth around 10^5 can exceed the ~1 MB default stack; iterate or raise the limit.`,mistakes:[`No base case, or one the recursion never reaches → infinite recursion → stack overflow.`,`Recursing on the same size — the instance never shrinks.`,`Exponential recomputation — the same subproblem solved thousands of times. Memoise.`,`Deep recursion overflowing the stack; convert to an explicit stack or increase it.`,"Passing large containers by value on every call — take `const&`.",`Backtracking: forgetting to undo the choice after the recursive call returns.`],edgeCases:[`n = 0 / empty input — the base case must return directly, not recurse.`,`Single element.`,`A branch over an empty range — return the identity (0 for a sum, empty for a list).`,`Recursion depth exactly at the stack limit.`],exercises:[`Print all subsets of {1..n} with include/exclude recursion.`,`Count the ordered ways to write n as a sum of 1s and 2s — naive, then memoise (it is Fibonacci).`,`Sum every integer in an arbitrarily nested list.`,`Implement merge sort.`],practice:[{name:`CSES — Creating Strings`,tag:`CSES`,url:`https://cses.fi/problemset/task/1622`},{name:`CSES — Apple Division`,tag:`CSES`,url:`https://cses.fi/problemset/task/1623`},{name:`LeetCode 46 — Permutations`,tag:`LC Medium`,url:`https://leetcode.com/problems/permutations/`}],combineNote:`Multi-branch recursion plus "undo after the call" is backtracking. Plus memoisation it is top-down DP — the same recursion, each distinct subproblem solved once. Recursion over a graph’s adjacency is DFS; over a tree it is tree traversal, and combining the children’s answers on the way back up is tree DP. Divide and conquer is recursion whose branches partition the input.`,prerequisites:[],related:[`backtracking`,`dfs`,`trees`,`dp-fundamentals`,`memoization-vs-iterative`],combinesWith:[`backtracking`,`memoization-vs-iterative`,`dfs`],next:[`backtracking`,`dfs`]}];function c(e,t,n,r,i,a,o={}){return{id:t,title:n,titleKa:r,category:e,priority:i,order:a,prerequisites:o.prerequisites??[],related:o.related??[],combinesWith:o.combinesWith??[],next:o.next??[]}}var l=[{id:`sorting-techniques`,title:`Sorting-based Techniques`,titleKa:`დახარისხების ხრიკები`,category:`core`,priority:`essential`,order:1,whatIs:'Not "how to sort" — `sort()` does that — but the meta-move: order the data (or a copy, or an index list) by a chosen key so that an ordering property makes the rest of the problem linear or easy.',intuition:`Many problems are hard only because the input is in arbitrary order. Impose an order that puts the elements you need to compare next to each other, and a nested loop becomes a single sweep.`,whyItWorks:'After sorting, "the next relevant element" is always adjacent or reachable by a monotone pointer. The sort costs `O(n log n)` once and buys `O(n)` or `O(n log n)` for the main logic instead of `O(n^2)`. The skill is choosing the key — by value, by a pair, by start time, by a custom comparator.',naive:"Comparing every pair to find the two closest numbers, or checking every interval against every other for overlap, is `O(n^2)`. Sort by the right key and the answer is between adjacent elements, or falls out of one left-to-right pass with a running state: `O(n log n)`, dominated by the sort.",whenToUse:[`"Closest pair", "minimum difference", "k-th smallest" — sort, then adjacent or indexed`,`Intervals — sort by start, or by end for a greedy`,`"Match / assign greedily" — sort both sides and walk them together`,`Offline queries — sort the queries into a favourable processing order`,`Sorting by a custom key: (deadline, then penalty), (ratio), (end time)`],signals:[`"minimum / maximum difference between any two", "closest"`,`"intervals", "segments", "meetings", "ranges" needing pairing or overlap counts`,`"you may answer the queries in any order" (offline)`,`An O(n^2) pair comparison that a 1-D order would make adjacent`,`"sort the ... by ..." is often literally the first line of the editorial`],walkthrough:[`Minimum absolute difference between any two of a = [8, 1, 5, 12, 3].`,`Naive: check all 10 pairs.`,`Sort → [1, 3, 5, 8, 12]. If x < y < z then y is closer to both than x is to z, so the closest pair must be adjacent.`,`Adjacent diffs: 2, 2, 3, 4 → minimum 2. One pass over n−1 pairs.`],cpp:[{caption:`Sort then sweep: merge overlapping intervals`,code:`#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; cin >> n;
    vector<pair<long long,long long>> v(n);
    for (auto& [l, r] : v) cin >> l >> r;
    sort(v.begin(), v.end());                 // by start, then end

    vector<pair<long long,long long>> merged;
    for (auto& [l, r] : v) {
        if (!merged.empty() && l <= merged.back().second)
            merged.back().second = max(merged.back().second, r);   // extend
        else
            merged.push_back({l, r});                              // new block
    }
    for (auto& [l, r] : merged) cout << l << " " << r << "
";
}`},{caption:`Sort an index array — keep the original data in place`,code:`vector<int> idx(n);
iota(idx.begin(), idx.end(), 0);              // 0, 1, 2, ...
sort(idx.begin(), idx.end(),
     [&](int i, int j) { return a[i] < a[j]; });  // strict "<", never "<="
// now a[idx[0]] is the smallest, and idx[k] is its original position`}],time:`O(n log n) for the sort; the technique on top is usually O(n) or O(n log n). Dominated by the sort.`,space:`O(n) if you sort a copy or an index array; O(log n) stack for the sort; O(1) extra if you sort in place and only sweep.`,mistakes:["Sorting the values but needing the original indices — sort an index array, or sort `(value, index)` pairs.","A comparator that is not a strict weak ordering — `return a <= b;` can crash `std::sort`. Use strict `<`.",`Sorting by the wrong key — by start when the greedy needs end, or the reverse.`,"Overflow in a ratio comparator (`a.x * b.y`) — cross-multiply in `long long`.",`Forgetting that sorting destroys an input order a later part of the problem still needs.`],edgeCases:[`n ≤ 1 — nothing to compare; handle before the sorting logic.`,`All elements equal — adjacent differences are 0; the comparator must stay strict.`,`Already sorted or reverse sorted — still O(n log n), no special case.`,"Duplicate keys under a custom comparator — decide whether ties need a secondary key or `stable_sort`."],exercises:[`Given points on a line, find the two closest.`,`Merge a list of intervals.`,`Sort n items by value/weight ratio (the fractional-knapsack setup).`,`Sort strings by length, breaking ties lexicographically.`],practice:[{name:`CSES — Restaurant Customers`,tag:`CSES`,url:`https://cses.fi/problemset/task/1619`},{name:`CSES — Movie Festival`,tag:`CSES`,url:`https://cses.fi/problemset/task/1629`},{name:`Codeforces 489B — BerSU Ball`,tag:`CF 1200`,url:`https://codeforces.com/problemset/problem/489/B`}],combineNote:"Sorting is the setup for two pointers (both pointers assume order), for binary search and `lower_bound` (need a sorted array), and for greedy (sort by the exchange-argument key, then take in order). Coordinate compression is sorting the distinct values and replacing each by its rank. Sweep-line sorts the events by coordinate and processes them in order.",prerequisites:[`two-pointers`],related:[`two-pointers`,`binary-search`,`greedy`,`coordinate-compression`],combinesWith:[`two-pointers`,`binary-search`,`greedy`],next:[`coordinate-compression`,`greedy`]},{id:`coordinate-compression`,title:`Coordinate Compression`,titleKa:`კოორდინატების შეკუმშვა`,category:`core`,priority:`important`,order:2,whatIs:`Replace a set of values that spans a huge range (up to 10^9 or 10^18) but has only n distinct entries with their ranks 0..k−1. Order is preserved, so any order-based structure now fits in an array of size k.`,intuition:`The actual magnitudes rarely matter — only the relative order does. So relabel the values 0, 1, 2, ... by their position in sorted order.`,whyItWorks:'Sort the distinct values; the rank of a value is its index in that list, found by `lower_bound` in `O(log k)`. Ranks are a bijection that preserves `<`, so every comparison, every "count values in [l, r]", every prefix structure gives the same answer on ranks as on the originals — but now the index space is `k`, not 10^9.',naive:"You want a Fenwick tree or a frequency array indexed by value, but the values reach 10^9 and the array will not allocate. A `map` works but adds a log factor and a heavy constant and cannot do the O(1) prefix tricks. Compression shrinks the index space to n so the fast array-based structure applies unchanged.",whenToUse:[`You need an array / Fenwick / segment tree indexed by value, but values are huge`,`"Count inversions", "for each element count smaller elements to its right"`,`Values are huge but only their order matters — ranks, medians, "k-th distinct"`,`2D problems — compress each axis independently`],signals:[`"values up to 10^9" (or 10^18, or "coordinates") together with a need to index by them`,`"count inversions", "count pairs (i, j) with i < j and a[i] > a[j]"`,`"how many values are less than x" over many queries, x from a huge range`,`A Fenwick / segment-tree idea that only fails because the value range is too big`],walkthrough:[`a = [100, 5, 100, 999999, 5].`,`Distinct, sorted: [5, 100, 999999].`,`rank(5) = 0, rank(100) = 1, rank(999999) = 2, each via lower_bound.`,`Compressed array: [1, 0, 1, 2, 0].`,`A Fenwick tree of size 3 can now count "values ≤ x seen so far" during a sweep — impossible on the raw values.`],cpp:[{caption:`The compression itself — a reusable helper`,code:`#include <bits/stdc++.h>
using namespace std;

// returns the rank array; \`sorted\` maps a rank back to its original value
vector<int> compress(const vector<long long>& a, vector<long long>& sorted) {
    sorted = a;
    sort(sorted.begin(), sorted.end());
    sorted.erase(unique(sorted.begin(), sorted.end()), sorted.end());  // distinct!
    vector<int> rank(a.size());
    for (size_t i = 0; i < a.size(); i++)
        rank[i] = lower_bound(sorted.begin(), sorted.end(), a[i]) - sorted.begin();
    return rank;
}`},{caption:`Its canonical use: count inversions (Fenwick tree covered in its own topic)`,code:`// bit[] is a Fenwick tree of size k; add(i) and query(i) are its O(log k) ops
long long inversions = 0;
vector<int> r = compress(a, sorted);
for (int i = (int)r.size() - 1; i >= 0; i--) {
    inversions += query(r[i] - 1);   // how many already-seen values are strictly smaller
    add(r[i]);                        // this value is now to the right of everything earlier
}`}],time:`O(n log n) to sort and dedupe; O(log k) per rank lookup. Dominated by the sort.`,space:`O(n) for the sorted distinct list and the rank array.`,mistakes:["Using `sort` without `unique` — ranks then skip numbers and the compressed range has gaps.","Off-by-one: `lower_bound(...) - begin()` is the rank; `upper_bound` shifts everything by one.",`Needing to map a rank back to a value and not keeping the sorted distinct list.`,`Comparing against a query value that was not in the original set — insert query values into the compression too, or reason carefully about "between two ranks".`,`Assuming compression preserves gaps — "is a[i] + 1 present" is not "rank + 1".`],edgeCases:[`All values equal — one distinct value, every rank 0, compressed size 1.`,`Values already 0..n−1 — compression is the identity, harmless.`,`Negative values — sorting handles them; ranks stay 0-based.`,`Query values outside the set — decide up front whether to add them.`,`n = 0 — empty distinct list, nothing to rank.`],exercises:[`Compress an array and verify the relative order is unchanged.`,`Count inversions in an array with values up to 10^9.`,`Given segments with huge endpoints, compress them and mark covered cells in a difference array.`,`For each element, how many distinct smaller values appear before it.`],practice:[{name:`LeetCode 315 — Count of Smaller Numbers After Self`,tag:`LC Hard`,url:`https://leetcode.com/problems/count-of-smaller-numbers-after-self/`},{name:`CSES — Nested Ranges Count`,tag:`CSES`,url:`https://cses.fi/problemset/task/2170`},{name:`Codeforces 1042D — Petya and Array`,tag:`CF 1800`,url:`https://codeforces.com/problemset/problem/1042/D`}],combineNote:"Coordinate compression is sorting plus `lower_bound` — the adapter that lets a Fenwick tree or segment tree work when the value range is too large to index. It is the standard first step for offline range-count problems, sweep-line over large coordinates, and 2D problems (compress each axis). Everything downstream — prefix sums, difference arrays, BIT — is unchanged.",prerequisites:[`sorting-techniques`,`frequency-arrays`],related:[`sorting-techniques`,`binary-search`,`fenwick-tree`,`segment-tree`],combinesWith:[`fenwick-tree`,`segment-tree`,`prefix-sums`,`difference-arrays`],next:[`fenwick-tree`]},c(`core`,`backtracking`,`Backtracking`,`უკუდახევა`,`essential`,3,{prerequisites:[`recursion`],related:[`bitmask-enumeration`,`dfs`,`constructive`],combinesWith:[`bitmask-enumeration`,`meet-in-the-middle`],next:[`bitmask-enumeration`,`constructive`]}),c(`core`,`bitmask-enumeration`,`Bitmask Enumeration`,`ბიტმასკის ჩამოთვლა`,`important`,4,{prerequisites:[`bit-manipulation`,`backtracking`],related:[`bitmask-dp`,`meet-in-the-middle`],combinesWith:[`bitmask-dp`,`meet-in-the-middle`],next:[`bitmask-dp`,`meet-in-the-middle`]}),c(`core`,`constructive`,`Constructive Algorithms`,`კონსტრუქციული ალგორითმები`,`important`,5,{prerequisites:[`invariants`],related:[`greedy`,`invariants`],combinesWith:[`greedy`,`invariants`],next:[`greedy`]}),c(`core`,`invariants`,`Invariants`,`ინვარიანტები`,`important`,6,{prerequisites:[],related:[`constructive`,`greedy`],combinesWith:[`constructive`,`greedy`],next:[`constructive`]})],u=[c(`greedy-search`,`greedy`,`Greedy & Exchange Argument`,`ხარბი ალგორითმები`,`essential`,1,{prerequisites:[`sorting-techniques`],related:[`invariants`,`constructive`,`mst`],combinesWith:[`sorting-techniques`,`priority-queue`],next:[`binary-search-on-answer`]}),c(`greedy-search`,`binary-search-on-answer`,`Binary Search on Answer`,`ბინარული ძებნა პასუხზე`,`essential`,2,{prerequisites:[`binary-search`,`greedy`],related:[`ternary-search`,`prefix-sums`],combinesWith:[`greedy`,`prefix-sums`,`two-pointers`],next:[`ternary-search`]}),c(`greedy-search`,`ternary-search`,`Ternary Search`,`ტერნარული ძებნა`,`advanced`,3,{prerequisites:[`binary-search-on-answer`],related:[`binary-search`],combinesWith:[`binary-search-on-answer`],next:[`meet-in-the-middle`]}),c(`greedy-search`,`meet-in-the-middle`,`Meet in the Middle`,`შუაში შეხვედრა`,`advanced`,4,{prerequisites:[`bitmask-enumeration`,`sorting-techniques`],related:[`bitmask-dp`,`two-pointers`],combinesWith:[`two-pointers`,`binary-search`],next:[]})],d=[c(`graphs`,`bfs`,`BFS`,`BFS`,`essential`,1,{prerequisites:[`recursion`],related:[`dfs`,`connected-components`,`01-bfs`],combinesWith:[`connected-components`,`flood-fill`],next:[`dfs`,`connected-components`]}),c(`graphs`,`dfs`,`DFS`,`DFS`,`essential`,2,{prerequisites:[`recursion`],related:[`bfs`,`trees`,`topological-sort`],combinesWith:[`connected-components`,`topological-sort`,`tree-dp`],next:[`connected-components`,`topological-sort`,`trees`]}),c(`graphs`,`connected-components`,`Connected Components`,`ბმული კომპონენტები`,`essential`,3,{prerequisites:[`bfs`,`dfs`],related:[`dsu`,`flood-fill`],combinesWith:[`dsu`,`flood-fill`],next:[`flood-fill`,`dsu`]}),c(`graphs`,`flood-fill`,`Flood Fill`,`შევსება`,`important`,4,{prerequisites:[`bfs`,`connected-components`],related:[`dfs`,`connected-components`],combinesWith:[`connected-components`],next:[`dijkstra`]}),c(`graphs`,`topological-sort`,`Topological Sort`,`ტოპოლოგიური დახარისხება`,`essential`,5,{prerequisites:[`dfs`],related:[`dfs`,`dp-fundamentals`],combinesWith:[`dp-fundamentals`,`dfs`],next:[`dijkstra`]}),c(`graphs`,`dijkstra`,`Dijkstra's Algorithm`,`დейკსტრა`,`essential`,6,{prerequisites:[`bfs`,`priority-queue`],related:[`01-bfs`,`mst`],combinesWith:[`priority-queue`,`binary-search-on-answer`],next:[`01-bfs`,`mst`]}),c(`graphs`,`01-bfs`,`0–1 BFS`,`0–1 BFS`,`important`,7,{prerequisites:[`bfs`,`dijkstra`],related:[`dijkstra`],combinesWith:[`dijkstra`],next:[`mst`]}),c(`graphs`,`mst`,`Minimum Spanning Tree`,`მინიმალური დამფარავი ხე`,`important`,8,{prerequisites:[`dsu`,`sorting-techniques`],related:[`dsu`,`greedy`],combinesWith:[`dsu`,`greedy`],next:[]}),c(`graphs`,`dsu`,`Disjoint Set Union`,`გაერთიანება-პოვნა`,`essential`,9,{prerequisites:[`connected-components`],related:[`mst`,`connected-components`],combinesWith:[`mst`,`sorting-techniques`,`coordinate-compression`],next:[`mst`]}),c(`graphs`,`trees`,`Trees & Tree Traversal`,`ხეები`,`essential`,10,{prerequisites:[`dfs`],related:[`tree-dp`,`lca`],combinesWith:[`tree-dp`,`lca`,`dfs`],next:[`lca`,`tree-dp`]}),c(`graphs`,`lca`,`Lowest Common Ancestor`,`უახლოესი საერთო წინაპარი`,`advanced`,11,{prerequisites:[`trees`,`sparse-table`],related:[`sparse-table`,`tree-dp`],combinesWith:[`sparse-table`,`tree-dp`],next:[]})],f=[c(`dp`,`dp-fundamentals`,`DP Fundamentals`,`DP საფუძვლები`,`essential`,1,{prerequisites:[`recursion`],related:[`memoization-vs-iterative`,`knapsack`],combinesWith:[`memoization-vs-iterative`],next:[`memoization-vs-iterative`,`knapsack`,`grid-dp`]}),c(`dp`,`memoization-vs-iterative`,`Memoization vs Iterative DP`,`მემოიზაცია vs იტერაციული`,`essential`,2,{prerequisites:[`dp-fundamentals`],related:[`dp-fundamentals`,`topological-sort`],combinesWith:[`topological-sort`],next:[`knapsack`,`grid-dp`]}),c(`dp`,`knapsack`,`Knapsack`,`ზურგჩანთა`,`essential`,3,{prerequisites:[`dp-fundamentals`],related:[`lis`,`bitmask-dp`],combinesWith:[`prefix-sums`,`frequency-arrays`],next:[`lis`,`grid-dp`]}),c(`dp`,`lis`,`Longest Increasing Subsequence`,`უგრძესი ზრდადი ქვემიმდევრობა`,`important`,4,{prerequisites:[`dp-fundamentals`,`binary-search`],related:[`knapsack`,`coordinate-compression`],combinesWith:[`binary-search`,`fenwick-tree`],next:[`grid-dp`]}),c(`dp`,`grid-dp`,`Grid DP`,`ბადის DP`,`essential`,5,{prerequisites:[`dp-fundamentals`],related:[`knapsack`,`prefix-sums`],combinesWith:[`prefix-sums`],next:[`bitmask-dp`,`tree-dp`]}),c(`dp`,`bitmask-dp`,`Bitmask DP`,`ბიტმასკის DP`,`advanced`,6,{prerequisites:[`bitmask-enumeration`,`dp-fundamentals`],related:[`meet-in-the-middle`,`knapsack`],combinesWith:[`meet-in-the-middle`],next:[`tree-dp`]}),c(`dp`,`tree-dp`,`Tree DP`,`DP ხეზე`,`advanced`,7,{prerequisites:[`trees`,`dp-fundamentals`],related:[`dfs`,`lca`],combinesWith:[`dfs`,`lca`],next:[]})],p=[c(`data-structures`,`stack-queue-deque`,`Stack / Queue / Deque`,`სტეკი / რიგი / დეკი`,`essential`,1,{prerequisites:[],related:[`monotonic-stack`,`monotonic-queue`,`priority-queue`],combinesWith:[`bfs`,`dfs`],next:[`priority-queue`,`monotonic-stack`]}),c(`data-structures`,`priority-queue`,`Priority Queue`,`პრიორიტეტული რიგი`,`essential`,2,{prerequisites:[`stack-queue-deque`],related:[`dijkstra`,`greedy`],combinesWith:[`dijkstra`,`greedy`],next:[`set-map`]}),c(`data-structures`,`set-map`,`Set / Map`,`სიმრავლე / ასოციაცია`,`essential`,3,{prerequisites:[`stack-queue-deque`],related:[`frequency-arrays`,`coordinate-compression`],combinesWith:[`sliding-window`,`two-pointers`],next:[`monotonic-stack`]}),c(`data-structures`,`monotonic-stack`,`Monotonic Stack`,`მონოტონური სტეკი`,`important`,4,{prerequisites:[`stack-queue-deque`],related:[`monotonic-queue`,`sliding-window`],combinesWith:[`sliding-window`],next:[`monotonic-queue`]}),c(`data-structures`,`monotonic-queue`,`Monotonic Queue`,`მონოტონური რიგი`,`important`,5,{prerequisites:[`stack-queue-deque`,`sliding-window`],related:[`monotonic-stack`,`sliding-window`],combinesWith:[`sliding-window`],next:[`sparse-table`]}),c(`data-structures`,`segment-tree`,`Segment Tree`,`სეგმენტ-ხე`,`advanced`,6,{prerequisites:[`recursion`,`coordinate-compression`],related:[`fenwick-tree`,`sparse-table`],combinesWith:[`coordinate-compression`,`binary-search`],next:[`fenwick-tree`]}),c(`data-structures`,`fenwick-tree`,`Fenwick Tree`,`ფენვიკის ხე`,`advanced`,7,{prerequisites:[`prefix-sums`,`bit-manipulation`],related:[`segment-tree`,`coordinate-compression`],combinesWith:[`coordinate-compression`,`difference-arrays`],next:[`sparse-table`]}),c(`data-structures`,`sparse-table`,`Sparse Table`,`იშვიათი ცხრილი`,`advanced`,8,{prerequisites:[`bit-manipulation`],related:[`segment-tree`,`lca`],combinesWith:[`lca`,`binary-search`],next:[`lca`]})],m=[c(`math`,`gcd-lcm`,`GCD / LCM`,`უდიდესი საერთო გამყოფი`,`essential`,1,{prerequisites:[],related:[`prime-factorization`,`modular-arithmetic`],combinesWith:[`modular-arithmetic`],next:[`sieve`]}),c(`math`,`sieve`,`Sieve of Eratosthenes`,`ერატოსთენეს საცერი`,`essential`,2,{prerequisites:[`frequency-arrays`],related:[`prime-factorization`],combinesWith:[`prime-factorization`],next:[`prime-factorization`]}),c(`math`,`prime-factorization`,`Prime Factorization`,`მარტივ მამრავლებად დაშლა`,`important`,3,{prerequisites:[`sieve`,`gcd-lcm`],related:[`sieve`,`modular-arithmetic`],combinesWith:[`sieve`],next:[`modular-arithmetic`]}),c(`math`,`modular-arithmetic`,`Modular Arithmetic`,`მოდულური არითმეტიკა`,`essential`,4,{prerequisites:[`gcd-lcm`],related:[`fast-exponentiation`],combinesWith:[`fast-exponentiation`],next:[`fast-exponentiation`]}),c(`math`,`fast-exponentiation`,`Fast Exponentiation`,`სწრაფი ახარისხება`,`essential`,5,{prerequisites:[`modular-arithmetic`,`bit-manipulation`],related:[`modular-arithmetic`],combinesWith:[`modular-arithmetic`],next:[]})],h=[`essential`,`important`,`advanced`];function g(e){return!!(e.whatIs&&e.cpp&&e.cpp.length>0)}var _={foundation:{en:`Foundation`,ka:`საფუძვლები`},core:{en:`Core Olympiad`,ka:`ბირთვი`},"greedy-search":{en:`Greedy / Search / Optimisation`,ka:`ხარბი და ძებნა`},graphs:{en:`Graphs`,ka:`გრაფები`},dp:{en:`Dynamic Programming`,ka:`დინამიური დაპროგრამება`},"data-structures":{en:`Data Structures`,ka:`მონაცემთა სტრუქტურები`},math:{en:`Contest Math`,ka:`ოლიმპიადური მათემატიკა`}},v=[`foundation`,`core`,`greedy-search`,`graphs`,`dp`,`data-structures`,`math`],y=[...s,...l,...u,...d,...f,...p,...m],b=new Map(y.map(e=>[e.id,e])),x=(()=>{let e=new Map;for(let t of v)e.set(t,[]);for(let t of y)e.get(t.category).push(t);for(let t of e.values())t.sort((e,t)=>e.order-t.order);return e})(),S={essential:0,important:1,advanced:2};function C(e,t){return i[e]>=i[t]}function w(e,t){let n=t.get(e.id)??a(e.id),r=e.prerequisites.map(e=>({id:e,p:t.get(e)})).filter(({p:e})=>!e||!C(e.status,`practicing`)).map(({id:e})=>b.get(e)).filter(e=>!!e);return{topic:e,progress:n,authored:g(e),unlocked:r.length===0,missingPrereqs:r}}function T(e){return v.map(t=>({category:t,topics:(x.get(t)??[]).map(t=>w(t,e))}))}function E(e){let t={"not-started":0,learning:0,practicing:0,mastered:0,review:0},n=0,r=0,i=0,a=0;for(let o of e)t[o.progress.status]++,n+=o.progress.solved,o.progress.reviewFlag&&r++,o.authored&&(i++,o.progress.status===`mastered`&&a++);return{total:e.length,authored:i,byStatus:t,solved:n,mastery:i===0?0:a/i,reviewCount:r}}function D(e){let t=e.filter(e=>e.authored),n=t.filter(e=>e.progress.status===`learning`).sort((e,t)=>(e.progress.lastStudiedAt??0)-(t.progress.lastStudiedAt??0));if(n[0])return n[0];let r=t.filter(e=>e.progress.reviewFlag).sort((e,t)=>(e.progress.lastStudiedAt??0)-(t.progress.lastStudiedAt??0));if(r[0])return r[0];let i=t.filter(e=>e.progress.status===`not-started`&&e.unlocked).sort((e,t)=>S[e.topic.priority]-S[t.topic.priority]||v.indexOf(e.topic.category)-v.indexOf(t.topic.category)||e.topic.order-t.topic.order);return i[0]?i[0]:t.find(e=>e.progress.status!==`mastered`)??null}function O(e,t){let n=t.query?.trim().toLowerCase();return e.filter(e=>!(t.category&&t.category!==`all`&&e.topic.category!==t.category||t.priority&&t.priority!==`all`&&e.topic.priority!==t.priority||t.status&&t.status!==`all`&&e.progress.status!==t.status||n&&!`${e.topic.title} ${e.topic.titleKa??``} ${e.topic.category}`.toLowerCase().includes(n)))}function k(e){return e.map(e=>b.get(e)).filter(e=>!!e)}function A(){let e=n(()=>r.ctProgress.toArray(),[]);return(0,o.useMemo)(()=>{let t=new Map((e??[]).map(e=>[e.topicId,e])),n=T(t),r=n.flatMap(e=>e.topics),i=new Map(r.map(e=>[e.topic.id,e]));return{roadmap:n,views:r,progressById:t,stats:E(r),recommended:D(r),loading:e===void 0,viewOf:e=>i.get(e),progressOf:e=>t.get(e)??a(e)}},[e])}function j(e){return n(()=>r.notes.where(`topicId`).equals(e).sortBy(`createdAt`),[e])}var M={"not-started":`დაუწყებელი`,learning:`ვსწავლობ`,practicing:`ვვარჯიშობ`,mastered:`ათვისებული`,review:`გასამეორებელი`},N={"not-started":`○`,learning:`◔`,practicing:`◑`,mastered:`●`,review:`⟳`},P={essential:`აუცილებელი`,important:`მნიშვნელოვანი`,advanced:`გაღრმავებული`},F={whatIs:`რა არის`,intuition:`ინტუიცია`,whyItWorks:`რატომ მუშაობს`,whenToUse:`როდის გამოვიყენოთ`,signals:`სად ვცნობთ ამოცანაში`,naive:`გულუბრყვილო გადაწყვეტა`,walkthrough:`ნაბიჯ-ნაბიჯ მაგალითი`,cpp:`C++ იმპლემენტაცია`,complexity:`სირთულე`,mistakes:`ტიპური შეცდომები`,edgeCases:`სასაზღვრო შემთხვევები`,prerequisites:`წინაპირობები`,related:`მონათესავე თემები`,combines:`როგორ ერწყმის სხვა ხრიკებს`,exercises:`მინი-სავარჯიშოები`,practice:`პრაქტიკული ამოცანები`,notes:`პირადი ჩანაწერები`,status:`სწავლის სტატუსი`};export{A as a,D as c,b as d,h as f,M as i,k as l,F as n,j as o,g as p,N as r,O as s,P as t,_ as u};