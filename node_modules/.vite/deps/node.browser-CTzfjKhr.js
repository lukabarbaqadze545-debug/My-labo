import { a as AnthropicError } from "./error-CgsyLo7K.js";
//#region node_modules/@anthropic-ai/sdk/internal/node.browser.mjs
/** Substituted for `./node` by the package.json `browser` field; every property access throws. */
function unavailable(module) {
	return new Proxy({}, { get(_target, property) {
		if (typeof property === "symbol") return void 0;
		throw new AnthropicError(`\`${module}.${property}\` is not available in this environment; it needs a Node.js-compatible runtime`);
	} });
}
unavailable("child_process");
unavailable("crypto");
var fs = unavailable("fs");
unavailable("os");
var path = unavailable("path");
unavailable("stream");
unavailable("util");
//#endregion
export { fs, path };
